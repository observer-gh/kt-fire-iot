name: Build and Push DataLake

on:
  push:
    branches: [main]
    paths:
      - "services/datalake/**"
  pull_request:
    branches: [main]
    paths:
      - "services/datalake/**"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push DataLake image
        uses: docker/build-push-action@v5
        with:
          context: ./services/datalake
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/kt-fire-iot-datalake:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/kt-fire-iot-datalake:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
